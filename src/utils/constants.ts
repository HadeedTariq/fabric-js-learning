type Actions = {
  SELECT: string;
  RECTANGLE: string;
  CIRCLE: string;
  SCRIBBLE: string;
};

export const DrawingActions: Actions = {
  SELECT: "SELECT",
  RECTANGLE: "RECTANGLE",
  CIRCLE: "CIRCLE",
  SCRIBBLE: "SCRIBBLE",
};
